# Copy to .env and fill in. .env is gitignored and never leaves this machine.
ANTHROPIC_API_KEY=sk-ant-...
PORT=5070

# Set only when deploying publicly. Gates /api/ask (the endpoint that spends money).
# Leave unset for local dev and the gate is a no-op.
# ACCESS_CODE=
